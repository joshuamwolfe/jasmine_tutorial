it('should calculate the monthly rate correctly', function () {
  expect(calculateMonthlyPayment().toEqual(224));
});


it("should return a result with 2 decimal places", function() {
  // ..
});

/// etc
